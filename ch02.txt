Intro
    왜 항상 기술 배경의 background, 해결하고자 한 문제를 찾기
    react.js UI makes interactive simply


Button click 횟수 세는 Application
in JS
    html에서 button 만들고 id나 class 줘서 querySelector
    button.addEventListener(callback)
    HTML update
    HTML에서 만들어서 JS로 보냄

in reactJS
    HTML을 페이지에 직접 작성하지 않음
    그 대신 JS코드를 사용
    JS에서 만들어서 HTML로 보냄(start from JS, end to HTML)
    유저에게 보여질 내용을 컨트롤

reactJS
    어플리케이션이 인터랙티브하도록 만들어주는 라이브러리

reactDOM
    react Element들을 html에 둘 수 있도록 해줌

JSX
    createElement 대체할만한 도구
    JS 확장한 문법
    HTML이랑 생긴게 비슷함
    {Js code}
    cf) pug #{js code}